package com.example.showenchantments.mixin;

/**
 * Arquivo deixado propositalmente vazio.
 *
 * A renderização acima do aldeão é feita via HUD usando GameRenderer.project,
 * sem mixins de renderizador (no 1.21.11 a pipeline usa RenderStates).
 */
public final class VillagerEntityRendererMixin {
	private VillagerEntityRendererMixin() {}
}
